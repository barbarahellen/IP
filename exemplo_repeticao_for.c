#include <stdio.h>

int main(void){

    int contador_for;

    for (contador_for = 0; contador_for< 10; contador_for++)
        printf("valor do contador for: %d\n", contador_for);
        printf("valor final do contador for: %d\n", contador_for); 

    return 0;
}